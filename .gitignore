# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# Real deployment config — copied from .env.example, may hold secrets. Never commit.
.env

# Claude Code local (personal) settings — not shared
.claude/settings.local.json

# Runtime output (harness runs, server artifact store / job DB)
runs/
report*.md
*.db
*.db-journal

# Local demo scenes / preview + real-render artifacts — not shared
examples/

# OS
.DS_Store
Thumbs.db
